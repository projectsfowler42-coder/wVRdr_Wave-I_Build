# Deploy trigger

This file exists to force a fresh push event on `main` so the GitHub Pages workflow reruns against the current repository state.

- Date: 2026-04-14
- Reason: force a new deploy after settings correction
